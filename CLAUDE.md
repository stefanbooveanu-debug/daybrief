@AGENTS.md

## Claude Code

- Before multi-file refactors, run `flutter analyze` and confirm a clean working tree.
- Prefer plan mode for changes under `lib/services/` or Firebase integration.
- After Dart edits, rely on the PostToolUse `dart format` hook; still run `flutter analyze` before marking work done.

@.claude/rules/dart.md
